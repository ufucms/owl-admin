<?php

use Illuminate\Routing\Router;
use Illuminate\Support\Facades\Route;
use {{Namespace}} AS {{moduleUC}};

Route::group([
    'as'         => '{{module}}',
    'domain'     => config('{{module}}.admin.route.domain'),
    'prefix'     => config('{{module}}.admin.route.prefix'),
    'middleware' => config('{{module}}.admin.route.middleware'),
], function (Router $router) {
    $router->resource('dashboard', {{moduleUC}}\HomeController::class);
    $router->resource('system/settings', {{moduleUC}}\SettingController::class);
});
